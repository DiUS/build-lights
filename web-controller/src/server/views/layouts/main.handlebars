<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Build Lights</title>
    <link rel="stylesheet" href="/static/css/foundation.css" media="screen" title="no title">
    <style media="screen">
      html {
        height: 100%;
      }

      body {
        display: flex;
        flex-direction: column;
        min-height: 100%;
        min-width: 30em;
      }

      .container {
        flex: 1;
        margin: 1em auto;
        max-width: 60em;
        width: 100%;
      }

      .container .representation {
        padding: 0 10px;
      }

      header {
        background-color: #21366C;
        height: 5em;
        text-align: right;
      }

      header ul {
        background-image: url('/static/img/dius_logo.png');
        background-position: 0 50%;
        background-size: contain;
        background-repeat: no-repeat;
        height: 100%;
        list-style: none;
      }

      header ul.container {
        margin: 0 auto;
      }

      header ul li {
        display: inline-block;
        line-height: 3.125em;
        margin-top: 0.625em;
        text-align: center;
        width: 8em;
      }

      header ul li a {
        background-color: rgba(163, 169, 172, 0.5);
        border-radius: 0.1875em;
        color: #666;
        display: block;
        font-weight: bold;
        letter-spacing: 1px;
        margin: 0.3125em;
      }

      header ul li a.reboot {
        background-color: rgba(226, 204, 0, 0.5);
      }

      header ul li a.reboot:hover {
        background-color: rgba(226, 204, 0, 1);
        color: #333;
      }

      header ul li a.shutdown {
        background-color: rgba(164, 30, 34, 0.5);
      }

      header ul li a.shutdown:hover {
        background-color: rgba(164, 30, 34, 1);
        color: #f5f5f5;
      }

      footer {
        background: #A3A9AC;
        font-size: 0.75em;
        padding: 0.625em;
        text-align: center;
      }
    </style>
  </head>
  <body>
    {{> header}}

    <div class="container">
      {{{body}}}
    </div>

    {{> footer}}
  </body>
  <script src="/static/js/bundle.js" charset="utf-8"></script>
</html>
