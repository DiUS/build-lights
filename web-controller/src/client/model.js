'use strict'

// import { render } from './state'

export const data = {
  activeTool: 'network',
  tools: [
    { network: {} },
    { jobs: {} }
  ],
  networkConfigured: false
}

// export function present (updatedData) {
//   render(updatedData)
// }
